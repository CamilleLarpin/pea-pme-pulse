services:
  nao:
    build:
      context: .
      dockerfile: Dockerfile.nao
    environment:
      - BETTER_AUTH_URL=http://34.77.145.245/nao
    volumes:
      - ./nao_config.yaml:/workspace/nao_config.yaml:ro
      - nao-workspace:/workspace/data
    restart: unless-stopped

volumes:
  nao-workspace:
