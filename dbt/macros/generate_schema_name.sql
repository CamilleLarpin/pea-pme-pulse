-- Use custom schema name directly (no target schema prefix).
-- Standard pattern for projects with explicit Silver/Gold dataset names.
{% macro generate_schema_name(custom_schema_name, node) -%}
    {%- if custom_schema_name is none -%}
        {{ target.schema }}
    {%- else -%}
        {{ custom_schema_name | trim }}
    {%- endif -%}
{%- endmacro %}
